name: Test SSH Connection

on:
  workflow_dispatch:  # Chạy thủ công từ GitHub UI
  push:
    branches:
      - test-ssh  # Hoặc tạo branch test riêng

jobs:
  test-connection:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Test cơ bản - Ping server
      - name: Test 1 - Check if server is reachable
        run: |
          echo "🔍 Testing connection to ${{ secrets.VPS_IP }}:2222"
          nc -zv ${{ secrets.VPS_IP }} 2222 || echo "❌ Port 2222 không truy cập được"

      # 2️⃣ Test SSH key format
      - name: Test 2 - Verify SSH key format
        run: |
          echo "🔑 Checking SSH key format..."
          echo "${{ secrets.VPS_SSH_KEY }}" > /tmp/test_key
          
          # Kiểm tra key có đúng format không
          if grep -q "BEGIN.*PRIVATE KEY" /tmp/test_key; then
            echo "✅ SSH key có header hợp lệ"
          else
            echo "❌ SSH key thiếu header!"
          fi
          
          if grep -q "END.*PRIVATE KEY" /tmp/test_key; then
            echo "✅ SSH key có footer hợp lệ"
          else
            echo "❌ SSH key thiếu footer!"
          fi
          
          # Đếm số dòng
          LINES=$(wc -l < /tmp/test_key)
          echo "📊 SSH key có $LINES dòng"
          
          rm /tmp/test_key

      # 3️⃣ Test SSH connection với verbose mode
      - name: Test 3 - SSH connection with verbose output
        continue-on-error: true
        run: |
          echo "🔐 Testing SSH connection với verbose mode..."
          mkdir -p ~/.ssh
          echo "${{ secrets.VPS_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          
          # Thêm host vào known_hosts
          ssh-keyscan -p 2222 ${{ secrets.VPS_IP }} >> ~/.ssh/known_hosts 2>&1 || echo "⚠️ ssh-keyscan failed"
          
          # Test kết nối với verbose
          ssh -vvv -p 2222 -i ~/.ssh/id_rsa \
            -o StrictHostKeyChecking=no \
            -o ConnectTimeout=30 \
            -o ServerAliveInterval=10 \
            ${{ secrets.VPS_USER }}@${{ secrets.VPS_IP }} \
            "echo '✅ SSH connection successful!'; whoami; pwd" 2>&1 | tee ssh_debug.log
          
          echo "📋 SSH debug log saved"

      # 4️⃣ Test với appleboy/ssh-action (action bạn đang dùng)
      - name: Test 4 - Using appleboy/ssh-action
        uses: appleboy/ssh-action@v1.2.0
        continue-on-error: true
        with:
          host: ${{ secrets.VPS_IP }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: 2222
          debug: true
          command_timeout: 30s
          script: |
            echo "✅ SSH via appleboy/ssh-action thành công!"
            echo "User: $(whoami)"
            echo "Home: $HOME"
            echo "Current dir: $(pwd)"
            echo "Disk space:"
            df -h | head -5
            echo "Docker status:"
            docker --version || echo "Docker chưa cài"

      # 5️⃣ Test SCP action
      - name: Test 5 - Create test file
        run: |
          echo "Test file from GitHub Actions" > test-file.txt
          date >> test-file.txt

      - name: Test 6 - Using appleboy/scp-action
        uses: appleboy/scp-action@v0.1.7
        continue-on-error: true
        with:
          host: ${{ secrets.VPS_IP }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: 2222
          debug: true
          timeout: 60s
          command_timeout: 30s
          source: "test-file.txt"
          target: "/home/${{ secrets.VPS_USER }}/test-upload"

      # 6️⃣ Verify file uploaded
      - name: Test 7 - Verify uploaded file
        uses: appleboy/ssh-action@v1.2.0
        continue-on-error: true
        with:
          host: ${{ secrets.VPS_IP }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: 2222
          script: |
            echo "📁 Checking uploaded file..."
            if [ -f "/home/${{ secrets.VPS_USER }}/test-upload/test-file.txt" ]; then
              echo "✅ File uploaded successfully!"
              cat "/home/${{ secrets.VPS_USER }}/test-upload/test-file.txt"
            else
              echo "❌ File upload failed"
              ls -la /home/${{ secrets.VPS_USER }}/
            fi

      # 7️⃣ Test multiple connections (giả lập real scenario)
      - name: Test 8 - Multiple sequential connections
        run: |
          echo "🔄 Testing multiple connections..."
          mkdir -p ~/.ssh
          echo "${{ secrets.VPS_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -p 2222 ${{ secrets.VPS_IP }} >> ~/.ssh/known_hosts 2>&1
          
          for i in {1..3}; do
            echo "🔗 Connection attempt $i/3"
            ssh -p 2222 -i ~/.ssh/id_rsa \
              -o StrictHostKeyChecking=no \
              -o ConnectTimeout=30 \
              ${{ secrets.VPS_USER }}@${{ secrets.VPS_IP }} \
              "echo 'Connection $i successful'" || echo "❌ Connection $i failed"
            sleep 2
          done

      # 8️⃣ Summary
      - name: Test Summary
        if: always()
        run: |
          echo "=================================="
          echo "📊 TEST SUMMARY"
          echo "=================================="
          echo "Host: ${{ secrets.VPS_IP }}"
          echo "Port: 2222"
          echo "User: ${{ secrets.VPS_USER }}"
          echo ""
          echo "✅ Nếu tất cả test pass = SSH hoạt động tốt"
          echo "❌ Nếu có test fail = cần check log chi tiết"
          echo "=================================="