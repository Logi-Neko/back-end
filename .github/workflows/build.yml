name: Build and Deploy LogiNeko on Windows

on:
  push:
    branches:
      - cicd # Hoặc nhánh bạn muốn dùng

jobs:
  build-and-deploy-windows:
    # 🎯 Yêu cầu job này phải chạy trên runner bạn vừa cài đặt
    runs-on: self-hosted

    steps:
      # 🧩 1. Lấy code từ branch
      - name: Checkout code
        uses: actions/checkout@v4

      # ☕ 2. Cài Java 21 (Temurin)
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      # ⚙️ 3. Build project bằng Maven
      - name: Build with Maven
        # Chạy lệnh Maven bằng PowerShell
        run: mvn clean package -DskipTests
        shell: pwsh

      # 🔐 4. Tạo file .env từ secrets GitHub (cú pháp cho PowerShell)
      - name: Create .env file
        shell: pwsh
        run: |
          @"
          KEYCLOAK_CLIENT_SECRET=${{ secrets.KEYCLOAK_CLIENT_SECRET }}
          KEYCLOAK_URL=${{ secrets.KEYCLOAK_URL }}
          KEYCLOAK_URL_WEB=${{ secrets.KEYCLOAK_URL_WEB }}
          KEYCLOAK_CLIENT_ID=${{ secrets.KEYCLOAK_CLIENT_ID }}
          KEYCLOAK_REALM=${{ secrets.KEYCLOAK_REALM }}
          kafka_server=${{ secrets.KAFKA_SERVER }}
          GOOGLE_CLIENT_ID=${{ secrets.GOOGLE_CLIENT_ID }}
          GOOGLE_CLIENT_SECRET=${{ secrets.GOOGLE_CLIENT_SECRET }}
          JWT_SECRET=${{ secrets.JWT_SECRET }}
          REDIRECT_URI=${{ secrets.REDIRECT_URI }}
          REDIRECT_URI_WEB=${{ secrets.REDIRECT_URI_WEB }}
          POSTGRES_PASSWORD_KEYCLOAK=${{ secrets.POSTGRES_PASSWORD_KEYCLOAK }}
          POSTGRES_USER=${{ secrets.POSTGRES_USER }}
          POSTGRES_PASSWORD=${{ secrets.POSTGRES_PASSWORD }}
          PAYOS_CLIENT_ID=${{ secrets.PAYOS_CLIENT_ID }}
          PAYOS_API_KEY=${{ secrets.PAYOS_API_KEY }}
          PAYOS_CHECKSUM_KEY=${{ secrets.PAYOS_CHECKSUM_KEY }}
          PAYOS_BASE_URL=${{ secrets.PAYOS_BASE_URL }}
          PAYOS_SUCCESS_PAGE=${{ secrets.PAYOS_SUCCESS_PAGE }}
          PAYOS_FAILURE_PAGE=${{ secrets.PAYOS_FAILURE_PAGE }}
          "@ | Out-File -FilePath .env -Encoding utf8

      # 🚀 5. Deploy trực tiếp bằng Docker Compose
      - name: Deploy with Docker Compose
        shell: pwsh
        run: |
          # Dừng lại nếu có lỗi xảy ra
          $ErrorActionPreference = "Stop"

          echo "🚀 Starting deployment directly on Windows host..."
          
          # Di chuyển vào thư mục chứa code mà runner đã checkout
          # Đây là nơi chứa file .jar, .env và docker-compose.yml của bạn
          cd "${{ github.workspace }}"
          
          echo "📁 Current directory:"
          ls
          
          # Đảm bảo Docker Desktop đang chạy (bước này bạn cần tự đảm bảo)
          echo "🛑 Stopping existing containers..."
          docker compose down --timeout 30
          
          echo "🧹 Cleaning up old images..."
          docker system prune -f
          
          echo "🏗️ Building and starting new containers..."
          docker compose up -d --build --force-recreate
          
          echo "⏳ Waiting for containers to stabilize..."
          Start-Sleep -Seconds 30
          
          echo "📊 Final container status:"
          docker compose ps
          
          echo "✅ Deployment on Windows completed successfully!"