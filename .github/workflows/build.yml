# Tên file: .github/workflows/build.yml

name: Build on GitHub, Deploy on Windows

on:
  push:
    branches:
      - main # Hoặc nhánh bạn muốn dùng

jobs:
  # ----------------------------------------------------------------
  # JOB 1: BUILD TRÊN MÁY ẢO CỦA GITHUB (UBUNTU)
  # ----------------------------------------------------------------
  build-and-push-image:
    runs-on: ubuntu-latest # Chạy trên máy chủ của GitHub, rất nhanh và mạnh
    steps:
      # 🧩 1. Lấy code từ branch
      - name: Checkout code
        uses: actions/checkout@v4

      # ☕ 2. Cài Java 21 và Maven
      - name: Set up JDK 21 and Maven
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          maven-version: '3.9.8'

      # ⚙️ 3. Build project bằng Maven
      - name: Build with Maven
        run: mvn clean package -DskipTests

      # 🔐 4. Đăng nhập vào Docker Hub
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }} # Cần tạo secret này
          password: ${{ secrets.DOCKERHUB_TOKEN }}   # Cần tạo secret này

      # 🐳 5. Build và đẩy Docker image lên Docker Hub
      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: . # Lấy Dockerfile từ thư mục gốc
          push: true
          # 👇 THAY THẾ 'your-username/logineko-be' bằng tên của bạn
          tags: alfred0324/logineko-be:latest

  # ----------------------------------------------------------------
  # JOB 2: DEPLOY TRÊN MÁY WINDOWS 11 CỦA BẠN
  # ----------------------------------------------------------------
  deploy-on-windows:
    runs-on: self-hosted # Chạy trên runner bạn đã cài đặt
    needs: build-and-push-image # Phải chờ job 1 thành công
    steps:
      # 🧩 1. Lấy code từ branch (để có file docker-compose.yml)
      - name: Checkout code
        uses: actions/checkout@v4

      # 🔐 2. Tạo file .env từ secrets (dùng PowerShell có sẵn của Windows)
      - name: Create .env file
        shell: powershell # Đổi từ pwsh thành powershell
        run: |
          @"
          KEYCLOAK_CLIENT_SECRET=${{ secrets.KEYCLOAK_CLIENT_SECRET }}
          KEYCLOAK_URL=${{ secrets.KEYCLOAK_URL }}
          KEYCLOAK_URL_WEB=${{ secrets.KEYCLOAK_URL_WEB }}
          KEYCLOAK_CLIENT_ID=${{ secrets.KEYCLOAK_CLIENT_ID }}
          KEYCLOAK_REALM=${{ secrets.KEYCLOAK_REALM }}
          kafka_server=${{ secrets.KAFKA_SERVER }}
          GOOGLE_CLIENT_ID=${{ secrets.GOOGLE_CLIENT_ID }}
          GOOGLE_CLIENT_SECRET=${{ secrets.GOOGLE_CLIENT_SECRET }}
          JWT_SECRET=${{ secrets.JWT_SECRET }}
          REDIRECT_URI=${{ secrets.REDIRECT_URI }}
          REDIRECT_URI_WEB=${{ secrets.REDIRECT_URI_WEB }}
          POSTGRES_PASSWORD_KEYCLOAK=${{ secrets.POSTGRES_PASSWORD_KEYCLOAK }}
          POSTGRES_USER=${{ secrets.POSTGRES_USER }}
          POSTGRES_PASSWORD=${{ secrets.POSTGRES_PASSWORD }}
          PAYOS_CLIENT_ID=${{ secrets.PAYOS_CLIENT_ID }}
          PAYOS_API_KEY=${{ secrets.PAYOS_API_KEY }}
          PAYOS_CHECKSUM_KEY=${{ secrets.PAYOS_CHECKSUM_KEY }}
          PAYOS_BASE_URL=${{ secrets.PAYOS_BASE_URL }}
          PAYOS_SUCCESS_PAGE=${{ secrets.PAYOS_SUCCESS_PAGE }}
          PAYOS_FAILURE_PAGE=${{ secrets.PAYOS_FAILURE_PAGE }}
          "@ | Out-File -FilePath .env -Encoding utf8

      # 🚀 3. Kéo image mới và khởi động lại container
      - name: Pull image and restart services
        shell: powershell # Đổi từ pwsh thành powershell
        run: |
          echo "🚀 Starting deployment on Windows..."
          
          echo "🐳 Pulling latest image from Docker Hub..."
          # Lệnh pull sẽ tự động kéo image được định nghĩa trong docker-compose.yml
          docker compose pull
          
          echo "🏗️ Recreating containers with the new image..."
          # Lệnh up sẽ chỉ tạo lại các service có image mới
          docker compose up -d --force-recreate
          
          echo "✅ Deployment completed!"